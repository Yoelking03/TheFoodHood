import { Injectable } from '@angular/core';
import supabase from './supabaseClient';

@Injectable({
  providedIn: 'root'
})
export class PedidoService {
  constructor() {}

  // Aquí irán funciones como crearPedido(), actualizarEstado(), etc.
}
