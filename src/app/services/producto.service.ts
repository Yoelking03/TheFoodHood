import { Injectable } from '@angular/core';
import supabase from './supabaseClient';

@Injectable({
  providedIn: 'root'
})
export class ProductoService {
  constructor() {}

  // Aquí irán funciones como obtenerProductos(), agregarProducto(), etc.

  
}
